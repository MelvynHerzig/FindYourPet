import { AdvertsEntity } from '../adverts/adverts.entity';

/**
 * Interface to implement in order to be a submittable as species entity
 */
export interface SpeciesInterface {
  id: number;
  name: string;
}
